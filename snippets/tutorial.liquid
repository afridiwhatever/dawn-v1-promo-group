{%- comment -%}
  Renders words passed onto it, super useful!

  Accepts:
  - word: {String} the word you want to pass to it
  -second_word: {String} you can pass another word if you want.

  Usage:
  {% render 'tutorial', {word: word_variable(string)} %}
{%- endcomment -%}